{
  "name": "my-electron-app",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "dependencies": {
    "electron": "^31.3.1",
    "electron-is-dev": "^3.0.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "concurrently \"npm run react-start\" \"npm run electron-start\"",
    "react-start": "cd legoatapp && BROWSER=none react-scripts start",
    "electron-start": "wait-on http://localhost:3000 && electron .",
    "build": "cd legoatapp && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "wait-on": "^6.0.0"
  }
}